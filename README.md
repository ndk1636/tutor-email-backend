# Tutor Email Backend (Vercel)

## Endpoint
POST /api/send-email

## Request Body (JSON)
{
  "toEmail": "example@example.com",
  "receiverName": "Ali",
  "senderName": "Nadeem",
  "messagePreview": "Hello! This is a test email sent from the tutor app."
}
